# -------------------------------
# 🔧 CONFIGURACIÓN GENERAL
# -------------------------------
spring.application.name=inventario
server.port=8080

# -------------------------------
# 🔐 CONFIGURACIÓN JWT
# -------------------------------
jwt.secret=mySecretKeyForJWTTokenGenerationThatIsLongEnough123456789
jwt.expiration=86400000

# -------------------------------
# 🌐 CONFIGURACIÓN CORS
# -------------------------------
cors.allowed-origins=*

# -------------------------------
# 💾 CONEXIÓN A BASE DE DATOS POSTGRESQL (Neon)
# -------------------------------
spring.datasource.url=jdbc:postgresql://ep-jolly-wind-ahahql34-pooler.c-3.us-east-1.aws.neon.tech:5432/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_QcaRd9Y4MEfj
spring.datasource.driver-class-name=org.postgresql.Driver

# -------------------------------
# 📊 CONFIGURACIÓN JPA / HIBERNATE
# -------------------------------
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# -------------------------------
# ⚙️ CONFIGURACIÓN DEL POOL DE CONEXIONES (Hikari)
# -------------------------------
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=10000
spring.datasource.hikari.connection-timeout=20
